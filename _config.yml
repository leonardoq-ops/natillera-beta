theme: jekyll-theme-minimal
title: Natillera Digital
description: Tradición y Seguridad en un Solo Lugar

# The Streamlit app (engine + dashboards) lives in app/ and deploys
# separately via Streamlit Community Cloud; the proof-upload API lives
# in api/ and deploys separately via Google Cloud Run. GitHub Pages
# must not try to build either.
exclude:
  - app/
  - api/
